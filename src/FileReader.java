import java.io.File;
import java.util.Scanner;

/**
 * Class that parses given file
 * 
 * @author Jaineel Nandu
 * @version 20201011
 */
public class FileReader {
    /**
     * Takes the files and parses for commands
     * 
     * @param filename
     *            name of the file to be parsed
     * @return
     *         boolean true if parsed correctly
     */
    public boolean beginParsing(String filename) {
        try {
            /*
                implementation hidden, contact Author
            */
            return true;
        }
        catch (Exception e) {
            e.printStackTrace();
            return false;
        }
    }
}
